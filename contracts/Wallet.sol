// SPDX-License-Identifier: GPL-3.0
pragma solidity >=0.7.0 <0.9.0;
import "./Token.sol";

contract Wallet{
    // Token public myToken ;
    // address owner;
    // uint unlockTime;
    // uint lockingTime;
    // uint amount;
    // constructor(address _owner,uint _unlockTime,uint _amount){

    //     owner = _owner;
    //     lockingTime = block.timestamp;
    //     unlockTime = block.timestamp + _unlockTime;
    //     amount = _amount;
    // }
    // function lockToken(address _tokenAddress,address _owner,uint _unlockTime,uint _amount) public{
    //     transfer(this, _amount);
    // }
    // function contractToken() public view returns(uint){
    //     return balanceOf(this);
    // }
}
